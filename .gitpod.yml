tasks:
  - init: |
      echo 'Installing dependencies.'
      cd catch-of-the-day
      npm install
    command: |
      echo 'Running the dev app'
      npm run dev
ports:
  - port: 3000
    onOpen: open-browser
github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
